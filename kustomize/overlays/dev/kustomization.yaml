bases:
  - ../../base

configMapGenerator:
  - name: multi-stage-react-app-example-config
    files:
      - config.js=dev.properties #add here the key config.js as you would when creating with kubectl
